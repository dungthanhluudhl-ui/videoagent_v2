---
name: vox-collage-video
description: Canonical workflow adapter for narrated investigative, legal, and documentary Remotion videos.
---

# VideoAgent 2 — Codex adapter

Before production work, read `.claude/skills/vox-collage-video/SKILL.md`. That file is
the single canonical workflow authority; this adapter does not duplicate its rules.

Use the canonical tools in `.claude/skills/vox-collage-video/scripts/` and explicitly
run their lifecycle checkpoint commands as directed by the canonical skill.

`.claude/settings.json` is Claude Code hook wiring. Do not claim it is active Codex
enforcement unless the current Codex runtime provides equivalent hook evidence. When
it does not, execute the canonical integrity scripts explicitly. Never claim “Codex
hooks passed” without actual hook evidence.